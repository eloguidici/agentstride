# @agentstride/mcp

Adapts MCP-shaped tools into AgentStride's existing tool model.

This package does not reimplement MCP. Connect a real MCP client in application code, then pass discovered tools through `createMcpToolBridge()`.
