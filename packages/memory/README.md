# @agentstride/memory

Small conversation memory adapters.

```ts
import { createInMemoryMemory } from "@agentstride/memory";
```

Redis/Postgres adapters can be added later without changing the `Memory` interface in core.
