{
  "name": "@agentstride/example-human-approval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Human approval pattern: propose → external approve → domain execute (no workflow engine)",
  "scripts": {
    "start": "npm run build -w @agentstride/core && node app.mjs",
    "test": "npm run build -w @agentstride/core && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@agentstride/core": "*",
    "zod": "^4.5.4"
  }
}
