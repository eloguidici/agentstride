# AgentStride demo MCP server

Tiny stdio MCP server used only to test `@agentstride/mcp`.

Tools:

- `echo` — returns the same message
- `add` — returns `a + b`

This is intentionally unrelated to any personal MCP servers outside this repository.
