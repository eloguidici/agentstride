{
  "name": "@agentstride/example-side-effect-idempotency",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Domain-level idempotent side-effect tools (no core idempotency framework)",
  "scripts": {
    "start": "npm run build -w @agentstride/core && node app.mjs",
    "test": "npm run build -w @agentstride/core && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@agentstride/core": "*",
    "zod": "^4.5.4"
  }
}
